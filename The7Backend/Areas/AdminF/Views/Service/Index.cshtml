@model IEnumerable<Service>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/AdminF/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

 <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Service Table</h4>
                    <a asp-area="AdminF" asp-controller="Service" asp-action="Create" class=" btn btn-success">Create</a>
                 
                  <div class="table-responsive">
                    <table class="table">
                      <thead>
                        <tr>
                          <th>Count</th>
                          <th>Title</th>
                      
                          <th>Settings</th>
                        </tr>
                      </thead>
                      <tbody>
                     

                         @foreach (var item in Model )
                                {
                                    count++;
                                       <tr>
                                    
                                    <td>@count</td>
                          
                          <td>@item.Title</td>
                      
                          <td>
                              <a asp-controller="Service" asp-action="Delete" asp-route-id="@item.Id" class=" btn btn-danger">Delete</a>
                               <a  asp-controller="Service" asp-action="Detail" asp-route-id="@item.Id"  class="btn btn-primary">Detail</a>
                                <a asp-controller="Service" asp-action="Update" asp-route-id="@item.Id" class=" btn btn-info">Update</a>
                          </td>
                          </tr>
                        }
                        
                     
                       
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
        
          </div>

